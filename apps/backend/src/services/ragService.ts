import { ChatRequest, ChatResponse } from '@ai-chatbot/shared/types/chat';

// Placeholder for future Graphiti + Neo4j RAG logic
export async function getRagAugmentedAnswer(_request: ChatRequest): Promise<ChatResponse> {
  return { answer: 'RAG not yet implemented.' };
}
